import { Priority as Priority0 } from './types/priority';
export type FindMount = {
	priority?: Priority0;
	avoid_water?: boolean;
	mount_distance?: number;
	start_delay?: number;
	target_needed?: boolean;
	within_radius?: number;
	max_failed_attempts?: number;
};
